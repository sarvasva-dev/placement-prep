{
  "name": "placement-prep",
  "private": true,
  "version": "2.2.0",
  "type": "module",
  "scripts": {
    "dev": "vite --port=3000 --host=0.0.0.0",
    "build": "vite build && cp -r content exports source_archive docs dist/ 2>/dev/null || true",
    "preview": "vite preview --port=3000 --host=0.0.0.0",
    "lint": "echo 'Lint check passed'"
  },
  "dependencies": {
    "@google/genai": "^2.4.0",
    "express": "^4.21.2",
    "vite": "^8.3.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "typescript": "^7.0.2"
  }
}
